#include "Functions.h"


void main() {
	Board B;
	B.Display();
	/*
	Board[1, 0] = 'h';
	Board[2, 0] = 'p';
	Board[1, 1] = 'k';

	for (int x = 0; x < 8; x++) {
		for (int  y= 0; y < 8; y++) {
			//cout << x << y;
			if (Board[x, y] = NULL) {
				cout << '.';
			}
			else {
				cout << Board[x, y];
			}
		}
		cout << endl;
	}*/

	system("pause");
}